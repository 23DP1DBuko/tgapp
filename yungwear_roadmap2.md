🗺️ Roadmap Item 1: UX Optimization – Replace Quick-View Eye Button with Long-Press GestureProblem Description:The current product cards feature an overlayed "eye" icon button in the bottom right corner of the image container to trigger a quick-view modal. On mobile screens, this floating button creates unnecessary visual noise, shrinks the viewable area of the clothing item, and increases accidental click risk due to its small touch target.Proposed Solution:Remove the Icon: Completely eliminate the floating eye icon button from the product card layout.Implement Long-Press Handler: Add a custom gesture event listener (onPointerDown / onTouchStart combination or a specialized hook) to the entire product card container.Interaction Logic:If a user taps normally, navigate them to the Product Details Page.If a user holds their finger down (long-press for (\sim 400\text{–}500\text{ ms})), intercept the navigation, trigger a subtle native haptic medium impact, and open the Quick-View modal.Releasing the long-press or dragging the finger away should smoothly dismiss the quick-view window or freeze it open based on premium app standards
.🗺️ Roadmap Item 2: Critical Bug – Like Button Event Regression (App Crash)Problem Description:Tapping the favorite/heart icon button on any product card in the catalog immediately breaks the application runtime context. It results in a generic React Error Boundary failure screen reading: "Something went wrong, the section encountered an unexpected error."Root Cause Analysis:This is a standard React state management regression or unhandled unhandled type fault. When the heart button is clicked:State Sync Mismatch: The component is likely calling a global update context hook or custom API dispatch handler that is receiving an unexpected parameter shape, an unauthenticated session state, or a null user profile object within the Telegram WebApp sandbox.Event Bubbling Execution: The event might still be failing to decouple cleanly from its parent navigation element, trying to update a local component state while simultaneously initiating an un-mounted page transition frame.Technical Fix Requirements:Isolate the local card handler using an explicit try/catch framework.Verify that the callback functions received via props are safely typed and checked before execution (e.g., onToggleLike?.(productId)).Wrap the action state update to ensure fallback metrics occur gracefully without killing the entire layout layer.
🗺️ Roadmap Item 3: UI Layout – Extraneous Padding and Excess Vertical Card LengthProblem Description:The layout on the Product Details page is visually inefficient. Despite removing the previous 2x2 statistics grid blocks, the container card remains excessively long, creating a massive area of dead, empty vertical space between the product description (aa) and the bottom action row. This forces important structural layout elements unnecessarily close to the fixed bottom navigation bar, making the screen feel unpolished.Proposed Solution:Remove Fixed Heights/Paddings: Audit the container element packaging the product data and remove any hardcoded vertical height (h-[Xpx]) or excessive padding (py-24, pb-32) properties.Implement Dynamic Content Fit: Force the card container to dynamically collapse tightly around its current active elements using standard block flow metrics (e.g., h-auto flex flex-col gap-4).Balance Element Spacing: Distribute whitespace proportionally below the photo so that title, price-action line, description, and drop-date stack naturally and leaves a clean, minimal gap before the action button.
🗺️ Roadmap Item 4: UI/UX Styling – "NOTIFY ME" Button Visual IncoherenceProblem Description:The current "NOTIFY ME" action button breaks the application's established high-street Y2K brand style guidelines. The button is styled as a plain, thin container with an understated solid gray border line and a muted, flat text weight. This makes it look like an unclickable secondary field or a disabled status plate, rather than an interactive premium button.Proposed Solution:Refactor to Brand Guidelines: Redesign the button using the established streetwear color palette system.Apply High-Street Aesthetic: Since this button handles out-of-stock notification loops, it should either carry a solid high-contrast dark indigo fill (bg-[#1C1622]) with an active neon-purple accent glow border line (border-[#A855F7]), or utilize a solid styling to match the prominent action states used elsewhere in the storefront app framework.Incorporate Haptic Feedback: Link Telegram's native feedback triggers (notificationOccurred('success')) to fire when a user successfully registers for a restock notification.
🗺️ Roadmap Item 5: Critical Bug – Heart/Like Component Interaction Crash on Product Details PageProblem Description:Clicking the standalone pill-styled Like count button on the details page (located right-aligned across from the price line) throws a fatal application execution crash. It triggers a generic React Error Boundary intercept layer displaying: "Something went wrong, the section encountered an unexpected error." This indicates that the critical context bug identified in the main catalog view has spilled into or been duplicated within the product detail screen view logic.Root Cause Analysis:Unhandled Event Execution Handler: The action handler assigned to the details view click event is failing to cleanly parse the updated product metrics layout properties.Missing State Fallback Strategy: The component is executing state modifications without confirming that the local state synchronizer or global query mutations handle the Telegram Mini App data packets safely inside this sub-view container.Technical Fix Requirements:Safeguard the detail view's heart tracking component state machine by implementing error-trapping clauses.Ensure that the state parameters parsing the updated values explicitly check the incoming item objects structure before refreshing the view layout thread.
🗺️ Roadmap Item 6: Architectural Bug – "Liked" Tab Misclassified as Storefront Filter StateProblem Description:The "Liked" tab is fundamentally broken because it inherits the filtering state machine from the main product catalog page. When a user clicks the "LIKED" navigation tab, the system treats it as an active search/category filter filter string rather than an isolated, user-specific collection.This causes two severe UI/UX regressions:False Indicator Bug: The filter icon in the search bar incorrectly displays a red notification dot, tricking the user into thinking they have manual filters active when they are simply viewing their favorites.Redundant Global Controls: The text items "0 PIECES" and the button "CLEAR ALL" are displayed. An intentional user favorites collection screen should never have global filter-wiping buttons.Proposed Technical Solution:Isolate Page State Context: Decouple the Liked view data collection cleanly from the global catalogFilters state array.Wipe Global Filter Indicators: Ensure that clicking the bottom navigation "LIKED" tab passes a clean, isolated query context that does not toggle the filter active state or display the search bar's red notification dot.
🗺️ Roadmap Item 7: UX Optimization – Remove Sorting Header & Force Chronological Array OrderProblem Description:The "Liked" tab currently displays the multi-row filtering header (LATEST / TRENDING text tabs stacked over the ALL / HOODIES / OUTERWEAR capsules). This layout is completely redundant here. Favorites do not need trending algorithmic sort blocks; users expect their saved items to be displayed in a predictable sequence.Proposed Technical Solution:Strip the Header Component: Completely hide or bypass the double-row filter carousel element when the active view state is set to the "LIKED" page view.Implement Chronological Sort Logic: Force the backend database query or local state slice to automatically organize favorited products by their save timestamp (likedAt date string), sorting from newest liked to oldest liked by default.
🗺️ Roadmap Item 8: UI Copy & Aesthetic Update – Replace "No Matches" with Friendly Empty StateProblem Description:When a user has saved zero items, the app currently displays a cold, database-style empty box reading "No matches." paired with a completely useless "CLEAR FILTERS" action button. This layout breaks user immersion and feels like an application search error rather than an empty wishlist.Proposed Technical Solution:Remove Error Elements: Delete the "No matches." card layout string and the "CLEAR FILTERS" button block entirely from this screen framework.Implement Premium Empty State: Render a clean, stylized high-contrast micro-copy text alignment block centered nicely on the viewport screen canvas.Updated Copy Guidelines:Primary Headline: "Seems you didn't like any product yet."Styling: Style this string in a soft, medium-contrast tracking font (text-gray-400 or text-zinc-500) to match our minimalist dark-mode high-street store vibe.
🗺️ Roadmap Item 9: Architectural Layout Error – Unshared and Inconsistent Navigation Header StylingProblem Description:The sub-navigation header button (← BACK TO CATALOG) is built into individual sub-pages as a detached layout element. This causes a major UI bug where the return path’s position, tracking font properties, and horizontal margin alignments vary wildly from page to page. Instead of using a unified, global header utility, each page independently declares a unique element, breaking navigation consistency across the entire storefront layout tree.Proposed Technical Solution:Create a Shared Global Top Header: Pull the structural back-navigation layout logic out of individual sub-screens (Catalog, Rewards, Details).Enforce Component Reusability: Build a single, shared top-bar layout wrapper component (<PageHeader backTarget="/catalog" title="..." />).Unified Visual Tracking: Enforce that the layout container uses an identical uppercase style matching our sleek street-wear font layout rules (text-[10px] tracking-widest text-zinc-400).
🗺️ Roadmap Item 10: UI Layout Clutter – Text Bloat inside Referral Context CardsProblem Description:The "YOUR REFERRAL LINK" block suffers from extreme text redundancy, causing unnecessary page lengths on small mobile screens. Repeating large paragraph fragments like "Share your unique referral link with friends. When they join through your link..." directly alongside an explanation block below it creates text bloat that users will not read.Proposed Technical Solution:Drastically Trim the Text Blocks: Strip the wordy instructional sentences completely out of the card. Reclaim this critical screen height so the application action targets are immediately visible.Compress into Direct Micro-Copy: Condense the entire explanatory paragraph text module into a single, punchy line of street-wear style typography text.Updated Copy Guidelines:Compressed Text: "Invite friends. Earn store credit and giveaway tickets."Styling: Mute this description down to a small, sleek style (text-xs text-zinc-400 mt-1) sitting right under the main box title.
🗺️ Roadmap Item 11: UI Layout Clutter – Redundant Order Statistics Cards for End UsersProblem Description:The top section of the user's order history page is crowded with an "Admin-style" 2x2 grid dashboard (ACTIVE: 9, COMPLETED: 4, etc.) and a large descriptive text paragraph ("Track your recent requests..."). While this data is critical for you in the Admin Panel, it is completely useless and overwhelming for an everyday customer. It pushes their actual order tracking cards down below the screen fold, making them scroll unnecessarily just to check on a purchase.Proposed Technical Solution:Wipe the Dashboard Grid: Completely delete the 2x2 metric cards from this user screen layout.Remove Explanatory Subtext: Remove the paragraph text block entirely.Direct Content Delivery: Reclaim 40% of the mobile viewport height so the user's actual order tracking elements are front and center the moment they open the tab.
🗺️ Roadmap Item 12: UI/UX Flow Optimization – Replace Broken Filter Capsules with Clean Sub-NavigationProblem Description:Because this screen lacks a dedicated sorting/filtering layout layer, a messy, text-heavy cluster of status capsules (ALL 16, ACTIVE, COMPLETED, CANCELLED) is forced into the middle of the scroll field. Stacking capsules vertically and horizontally in a raw grid creates serious interface clutter.Proposed Technical Solution:Implement a Horizontal Menu Slider: Convert these options into a single, clean horizontal tab bar (overflow-x: auto) with hidden scrollbars, exactly matching the storefront styling rules.Mute Inactive States: Keep unselected choices completely flat against the background, letting the active state light up using your brand's neon-purple accent color indicator line.
🗺️ Roadmap Item 13: UI Simplification – Heavy Customer Order Cards & Text OverloadProblem Description:The individual user order tracking cards are completely unformatted and dump an overwhelming wall of database strings (Order ID: PvI9r9..., Fulfillment: Meetup, Payment: Meetup Cash, multiple explanation sentences) directly onto the feed. Since tapping a card already correctly opens a detailed modal view, displaying all of this raw text on the main scroll list is completely redundant and makes the cards excessively tall.Proposed Technical Solution:Drastically Shrink Card Layout Height: Make each order row highly compact.Hide Dense Metadata: Hide the full Order ID string, fulfillment text rows, payment types, and explanation blocks by default. Move them strictly into the product detail modal view.Aesthetic 3-Element Header Grid: Structure each collapsed order card row using a minimal layout:Left Side: Bold item name (mark) and a highly abbreviated order identifier (#PvI9r9).Center Side: A clean visual progress timeline bar layout tracking fulfillment states (REQUEST ➔ CONFIRMED ➔ MEETUP ➔ DONE).Right Side: Order cost text (50 EUR) cleanly stacked above a stylized status pill sticker (NEW).
🗺️ Roadmap Item 14: Navigation Consistency – Unshared "Back to Catalog" Button Header PlacementProblem Description:The top BACK TO CATALOG return navigation button on this page is once again independently coded instead of utilizing a single global component structure. The text casing, touch target padding, and horizontal margins do not align with the header layout blocks on the other application sub-screens.Proposed Technical Solution:Inject the Global Header Component: Replace the native text button with the shared header module (<PageHeader backTarget="/catalog" />) engineered in Roadmap Item 9. This ensures your app navigation remains flawlessly synchronized as the user moves between pages.
🗺️ Roadmap Item 15: Functional Critical Bug – Checkout Actionable on Empty Cart StateProblem Description:The checkout pipeline lacks basic form validation and error checking. When the user's cart is completely empty (0 ITEMS), both checkout button components incorrectly remain fully interactive. Clicking either checkout button triggers a dead action path or redirects the customer to a broken checkout screen context, which creates an unprofessional user experience.Proposed Technical Solution:Implement Cart Length Validation: Wrap the checkout buttons in a strict conditional evaluation state (cartItems.length === 0).Enforce Native HTML Disabling: When the cart count is zero, add the native disabled attribute to the local button container and switch its styling to a flat, muted gray canvas layout.Intercept Click Handler Hooks: Ensure the click action event handlers explicitly check item counts to guarantee a checkout sequence can never execute with zero active clothing assets.
🗺️ Roadmap Item 16: UI Redundancy – Double "Checkout" Call-to-Action ButtonsProblem Description:The cart interface suffers from severe action bloating by rendering two completely separate "Checkout" buttons simultaneously on a single mobile screen context. You have a local gradient pill button (CHECKOUT) nested right inside the cart details box, sitting directly above a full-width, bright blue native Telegram Main Button displaying the exact same Checkout text instruction. This duplicate layout breaks visual hierarchy and looks uncoordinated.Proposed Technical Solution:Consolidate the Primary CTA Path: Pick one single layout pattern to handle user checkout actions.Utilize Telegram Native Main Button Engine (Recommended): Completely eliminate the local purple-to-red gradient pill button from inside the content box layout.Dynamic State Tracking Loop: Let Telegram's native, fixed bottom Main Button act as the sole transaction master element.If Cart is Empty: Hide the native Main Button (window.Telegram.WebApp.MainButton.hide()) or change its state to a disabled, flat layout format.If Cart Has Items: Display the native Main Button brightly, matching our brand’s neon-purple accent theme canvas, and set its label tracking string dynamically (e.g., CHECKOUT • €110.00).
🗺️ Roadmap Item 17: Navigation Overlap – Duplicate Redundant Return PathsProblem Description:The layout contains three separate return navigation controls crammed into a tiny viewport layout framework: a top standalone ← CATALOG button element, an internal local ← BACK pill button inside the card block, and the native Telegram back arrow in the application header interface. This excessive duplication creates action confusion on mobile screen dimensions.Proposed Technical Solution:Strip the Internal Box Button: Completely remove the dark ← BACK pill button from inside the cart details card layout row to reclaim immediate element breathing room.Enforce Global Header Synchronization: Replace the top text asset with your shared, unified structural header component module (<PageHeader backTarget="/catalog" />) engineered back in Roadmap Item 9.
🗺️ Roadmap Item 18: Backend Database Architecture – Missing "Campaigns" Database Entity & API SchemasProblem Description:The Admin Carousel Manager form fails to save new hero banner slides because the backend data layer completely lacks a database model, collection, or entity schema for handling campaigns and hero elements. While your system currently manages standard e-commerce structures (broadcasts, orders, products, promoCodes, telegramSubscribers), any creation request sent from the new "Growth" UI instantly fails because the API endpoint does not exist, maps to nothing, or returns unhandled error frames.Proposed Technical Solution:Database Schema Migration: Create a new backend collection/table explicitly named campaigns or heroSlides.Define Model Properties: Ensure the database model accurately maps your front-end form fields:id (auto-generated UUID / string)imageUrl (string mapping to your static media storage bucket destination)badgeText (string, optional)headline (string, required)subheading (string, optional)caption (string, optional)isVisible (boolean)sortOrder (integer)createdAt / updatedAt (timestamps)Expose CRUD API Routing: Develop the corresponding REST endpoints or RPC handlers (e.g., POST /api/admin/campaigns, GET /api/campaigns) to securely write, process, and fetch this dataset.
🗺️ Roadmap Item 19: Security & Authorization – Missing RBAC (Role-Based Access Control) Policies for Growth RoutesProblem Description:Submitting the form triggers a hard authentication failure code ("Insufficient permissions") even when executing the action from an verified, authenticated Administrator account session. This behavior indicates that the backend's access validation layers or interceptor hooks have not been extended to recognize the new sub-routes associated with the newly launched Growth engine sub-tabs.Proposed Technical Solution:Audit Endpoint Security Middleware: Review your API routing files where admin middleware is applied. Ensure the checking logic explicitly maps the admin flag check to the new campaign execution commands.Initialize Telegram User ID Validation: Double-check that your server-side session initializer is properly parsing and decrypting the incoming Telegram initialization data packet (window.Telegram.WebApp.initData) to confirm that your specific account ID matches your backend server's strict hardcoded super-admin array permissions.
🗺️ Roadmap Item 20: Frontend Media Handling – Image Upload Failure Error TrappingProblem Description:When attempting a local file image upload within the dashed dropzone area, the front-end layout immediately returns an unhandled upload error. Because the client code attempts a blind binary dispatch to a storage bucket before validating the endpoint access, any missing backend capability or auth handshake error causes the front-end file preview pipeline to break completely without dropping a useful error log.Proposed Technical Solution:Implement Cloud Storage Lifecycle Interception: Build a resilient, async file reader utility block that takes the local input file binary (FileList[0]), converts it locally to an instant object URL string (URL.createObjectURL(file)) for immediate frontend rendering, and traps network upload states securely.Wrap API Dispatches in Try/Catch Blocks: Isolate your cloud storage handler inside a protective wrapper. If a network drop or permission error happens, display a clear, context-specific warning notice (e.g., "Upload Failed: Backend Server Denied Authorization"), preventing your global React tree from encountering runtime execution errors.
🗺️ Roadmap Item 21: UI Layout Copy-Paste Bug – Replicated Broadcast View inside Polls TabProblem Description:The "POLLS" sub-navigation view is fundamentally broken because it copies the exact visual design and form layout from the "BROADCAST MESSAGE" screen. Selecting the Polls tab incorrectly renders the same text area input box (Write your broadcast message...) and action button (SEND TO ALL SUBSCRIBERS), rather than a functional interface to create interactive user polls or questionnaires.Proposed Technical Solution:Create an Isolated Poll Creator Component: Completely decouple the Polls screen logic from the Broadcast component tree.Build the Dynamic Form Structure: Replace the single text area field with a specialized creation form that maps multiple options dynamically:Question Input: A short text input box for the core query.Options Array Layout: A dynamic list field where the admin can tap a small + Add Option button to cleanly insert multiple poll answer choices (e.g., Option 1: "Black Hoodie", Option 2: "Purple Zip-Up").Update the Primary Action Call-to-Action: Change the button text from its broadcast statement to a clean, contextual string reading "LAUNCH PUBLIC POLL".
🗺️ Roadmap Item 22: Firestore Database Architecture – New "polls" Collection and Data SchemaProblem Description:The backend layer does not support polls because there is no corresponding data model or entity collection inside Firestore. To roll out interactive surveys to your user base, the system requires a clean document schema to write, store, and count live incoming answers.Proposed Technical Solution:Initialize a New Firestore Collection: Register a top-level collection explicitly titled polls.Enforce Document Property Types: Map each generated survey document according to this explicit structural schema definition:typescriptinterface FirestorePoll {
  id: string;               // Auto-generated Firestore document ID
  question: string;         // The core survey query text
  options: {
    id: string;             // Option unique identifier
    text: string;           // e.g., "Drop this Friday"
    voteCount: number;      // Integer tracking live user answers
  }[];
  isActive: boolean;        // Global toggle flag for visibility
  createdBy: string;        // Telegram User ID string of the creator admin
  createdAt: Date;          // Server timestamp marker
  votedUserIds: string[];   // Array of strings tracking Telegram IDs to prevent double-voting
}
Используйте код с осторожностью.Establish Client-Side Polling Actions: Develop the corresponding Firestore trigger functions (updateDoc using arrayUnion for votedUserIds and increment commands for voteCount) so that client interactions safely refresh without concurrency bugs.